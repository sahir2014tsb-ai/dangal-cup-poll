<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Dangal Cup</title>

<style>
body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: linear-gradient(black, #111);
    color: white;
    text-align: center;
}

/* HERO */
.hero {
    padding: 60px 20px;
    background: radial-gradient(circle at top, #222, black);
}

.hero h1 {
    font-size: 60px;
    color: gold;
    text-shadow: 0 0 20px gold;
}

.hero p {
    opacity: 0.8;
}

/* LEGACY */
.legacy {
    padding: 20px;
    font-size: 18px;
    opacity: 0.9;
}

/* POLL */
.poll {
    margin: 30px auto;
    padding: 20px;
    max-width: 500px;
    background: #1a1a1a;
    border: 2px solid gold;
    border-radius: 15px;
}

button {
    margin: 10px;
    padding: 10px 20px;
    border: none;
    border-radius: 10px;
    font-weight: bold;
    cursor: pointer;
}

.yes { background: limegreen; }
.maybe { background: orange; }
.no { background: red; }

.bar {
    height: 10px;
    background: #333;
    border-radius: 5px;
    margin: 5px 0;
}

.fill {
    height: 10px;
    background: gold;
    width: 0%;
}

/* TEAMS */
.teams {
    padding: 20px;
}

.grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 10px;
    padding: 10px;
}

.card {
    background: #222;
    border: 1px solid gold;
    padding: 10px;
    border-radius: 10px;
}

/* FOOTER */
footer {
    margin-top: 30px;
    padding: 10px;
    font-size: 12px;
    opacity: 0.7;
}
</style>
</head>

<body>

<div class="hero">
    <h1>DANGAL CUP</h1>
    <p>The Official 7th Grade Football League</p>
    <p><i>Play • Compete • Conquer</i></p>
</div>

<div class="legacy">
    <p>9th has Takkar Cup.</p>
    <p>11th has Pinde Cup.</p>
    <p>8th… we don’t talk about it.</p>
    <p><b>So 7th made Dangal Cup.</b></p>
</div>

<div class="poll">
    <h2>Should Dangal Cup happen?</h2>

    <button class="yes" onclick="vote('yes')">YES</button>
    <button class="maybe" onclick="vote('maybe')">MAYBE</button>
    <button class="no" onclick="vote('no')">NO</button>

    <p>Yes: <span id="y">0</span></p>
    <div class="bar"><div id="yb" class="fill"></div></div>

    <p>Maybe: <span id="m">0</span></p>
    <div class="bar"><div id="mb" class="fill"></div></div>

    <p>No: <span id="n">0</span></p>
    <div class="bar"><div id="nb" class="fill"></div></div>

    <p id="msg"></p>
</div>

<div class="teams">
    <h2>Teams</h2>
    <div class="grid">
        <div class="card">Manchester City Type</div>
        <div class="card">Manchester United Type</div>
        <div class="card">Real Madrid Type</div>
        <div class="card">Barcelona Type</div>
        <div class="card">Arsenal Type</div>
        <div class="card">Chelsea Type</div>
        <div class="card">PSG Type</div>
        <div class="card">Tottenham Type</div>
    </div>
</div>

<footer>
Made by a 7th grader with too much motivation ⚽
</footer>

<script>
let data = {yes:0, maybe:0, no:0};

function vote(type){
    if(localStorage.getItem("voted")){
        document.getElementById("msg").innerText = "You already voted 😏";
        return;
    }

    data[type]++;
    localStorage.setItem("voted", "true");

    update();
}

function update(){
    document.getElementById("y").innerText = data.yes;
    document.getElementById("m").innerText = data.maybe;
    document.getElementById("n").innerText = data.no;

    let total = data.yes + data.maybe + data.no;

    document.getElementById("yb").style.width = (data.yes/Math.max(total,1))*100 + "%";
    document.getElementById("mb").style.width = (data.maybe/Math.max(total,1))*100 + "%";
    document.getElementById("nb").style.width = (data.no/Math.max(total,1))*100 + "%";
}
</script>

</body>
</html>
